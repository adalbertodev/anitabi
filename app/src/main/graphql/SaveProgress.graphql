mutation SaveProgress($entryId: Int, $progress: Int, $status: MediaListStatus) {
    SaveMediaListEntry(id: $entryId, progress: $progress, status: $status) {
        id
        progress
        status
        updatedAt
    }
}